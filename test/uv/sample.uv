/*======================================
	Default inclusions required for
	 the language to function properly
=======================================*/

trait Drop {
	fn drop(this: @Self);
}

trait Clone {
	fn clone(this: @Self): Self;
}